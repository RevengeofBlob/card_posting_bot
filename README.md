# card_posting_bot
Discord bot that uses the Chronicles of Arcane card database to display cards in chat

## Set up
pip install discord.py
pip install BeautifulSoup4
pip install selenium

In your discord bot, turn "MESSAGE CONTENT INTENT" on